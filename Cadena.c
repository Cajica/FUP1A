#include <stdio.h>

int main()
{
	char cadena[50] ,Subcad[30];
	char i,palabra, pal, buscar,remplazar;
	printf("ingrese palabra\n");
	scanf(" %d", & palabra);
	printf("ingrese palabra\n");
	scanf(" %d", & pal);
	
		scanf("%s",cadena);
		scanf(" %c", & buscar);
		scanf(" %c", & remplazar);

for (i=0; i<50;i++)
{
	if (cadena [i] == buscar)
{
		cadena [i] == remplazar;
		printf(
	}
}

	return 0;
}

